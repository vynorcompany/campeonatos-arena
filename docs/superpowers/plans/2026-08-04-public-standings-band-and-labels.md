# Public Standings Band and Labels Plan

1. Add source contracts for `Ranking Geral` and league-only option labels in `tests/public-standings.test.ts`.
2. Update `src/lib/public-standings.ts` option labels without changing ids.
3. Update `src/app/globals.css` so the blue band is full-bleed and its inner content shares the page column alignment.
4. Run public standings tests, typecheck, production build, commit, and push `main`.
