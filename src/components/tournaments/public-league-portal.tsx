"use client";

import { useState, useTransition } from "react";
import { createLeagueChallengeAction, respondLeagueChallengeAction } from "@/lib/actions/league-challenges";

type Portal = NonNullable<Awaited<ReturnType<typeof import("@/lib/services/public-league-portal").getPublicLeaguePortal>>>;

export function PublicLeaguePortal({ arenaSlug, playerName, portal }: { arenaSlug: string; playerName: string; portal: Portal }) {
  const [message, setMessage] = useState("");
  const [pending, startTransition] = useTransition();
  const send = (form: HTMLFormElement) => startTransition(async () => { try { await createLeagueChallengeAction(new FormData(form)); setMessage("Convite enviado para a dupla."); } catch (error) { setMessage(error instanceof Error ? error.message : "Não foi possível enviar o convite."); } });
  const respond = (form: HTMLFormElement) => startTransition(async () => { try { await respondLeagueChallengeAction(new FormData(form)); setMessage("Resposta registrada."); } catch (error) { setMessage(error instanceof Error ? error.message : "Não foi possível responder ao convite."); } });

  return <section className="public-league-portal section-card stack-md"><header className="public-league-portal-header"><div><p className="eyebrow">ÁREA DO ATLETA</p><h2>Olá, {playerName}</h2><p className="muted">Acompanhe suas ligas e combine seus jogos.</p></div><span className="public-portal-badge">{portal.notifications.length} notificação{portal.notifications.length === 1 ? "" : "ões"}</span></header>{portal.notifications.length ? <section className="public-portal-notifications">{portal.notifications.map((notification) => <a href={notification.href || "#"} key={notification.id}><strong>{notification.title}</strong><span>{notification.message}</span></a>)}</section> : null}{portal.pairs.length ? <section className="public-portal-pairs">{portal.pairs.map((pair) => <article className="public-portal-pair" key={pair.id}><header><div><strong>{pair.name}</strong><span>{pair.eventName} · {pair.categoryName}{pair.groupName ? ` · ${pair.groupName}` : ""}</span></div><small>{pair.players.join(" · ")}</small></header>{pair.opponents.length ? <form onSubmit={(event) => { event.preventDefault(); send(event.currentTarget); }} className="public-challenge-form"><input type="hidden" name="arenaSlug" value={arenaSlug} /><input type="hidden" name="proposerPairId" value={pair.id} /><label>Dupla adversária<select name="opponentPairId" required>{pair.opponents.map((opponent) => <option value={opponent.id} key={opponent.matchId}>{opponent.name}</option>)}</select></label><label>Horário disponível<select name="slot" required onChange={(event) => { const [courtId, startsAt, durationMinutes] = event.target.value.split("|"); const form = event.currentTarget.form!; (form.elements.namedItem("courtId") as HTMLInputElement).value = courtId; (form.elements.namedItem("startsAt") as HTMLInputElement).value = startsAt; (form.elements.namedItem("durationMinutes") as HTMLInputElement).value = durationMinutes; }}>{portal.slots.map((slot) => <option value={slot.value} key={slot.value}>{slot.label}</option>)}</select></label><input type="hidden" name="courtId" defaultValue={portal.slots[0]?.value.split("|")[0] ?? ""} /><input type="hidden" name="startsAt" defaultValue={portal.slots[0]?.value.split("|")[1] ?? ""} /><input type="hidden" name="durationMinutes" defaultValue={portal.slots[0]?.value.split("|")[2] ?? "60"} /><button type="submit" className="button button-primary" disabled={pending || !portal.slots.length}>{pending ? "Enviando..." : "Desafiar dupla"}</button></form> : <p className="muted">Não há jogos pendentes contra outras duplas elegíveis.</p>}</article>)}</section> : <p className="muted">Você ainda não participa de nenhuma Liga ativa.</p>}<section className="public-challenge-list"><h3>Convites</h3>{portal.challenges.length ? portal.challenges.map((challenge) => <article id={`desafio-${challenge.id}`} key={challenge.id}><div><strong>{challenge.proposer} × {challenge.opponent}</strong><span>{challenge.court} · {challenge.proposedAt}</span><small>{challenge.status === "PENDING" ? "Aguardando resposta" : challenge.status === "ACCEPTED" ? "Aceito — aguardando arena" : "Recusado"}</small></div>{challenge.incoming && challenge.status === "PENDING" ? <div className="public-challenge-actions"><form onSubmit={(event) => { event.preventDefault(); respond(event.currentTarget); }}><input type="hidden" name="arenaSlug" value={arenaSlug} /><input type="hidden" name="challengeId" value={challenge.id} /><input type="hidden" name="response" value="ACCEPTED" /><button className="button button-primary" disabled={pending}>Aceitar</button></form><form onSubmit={(event) => { event.preventDefault(); respond(event.currentTarget); }}><input type="hidden" name="arenaSlug" value={arenaSlug} /><input type="hidden" name="challengeId" value={challenge.id} /><input type="hidden" name="response" value="REJECTED" /><button className="button" disabled={pending}>Recusar</button></form></div> : null}</article>) : <p className="muted">Nenhum convite recebido ou enviado.</p>}</section>{message ? <p className="public-booking-message" role="status">{message}</p> : null}</section>;
}
