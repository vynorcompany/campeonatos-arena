"use client";

import { useMemo, useState, useTransition } from "react";
import { addComandaProductAction, deleteComandaAction, finishComandaAction, updateComandaItemQuantityAction } from "@/lib/actions/comanda";

type Product = { id: string; name: string; priceCents: number; stockQuantity: number; category?: { name: string } | null };
type CommandItem = { id: string; quantity: number; totalCents: number; product: { name: string } };
type Payment = { id: number; paymentMethod: string; amount: string };
type Debt = { id: string; description: string; amountCents: number; dueDate: Date | null };

function money(value: number) { return new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" }).format(value / 100); }
function toCents(value: string) { return Math.round(Number(value.replace(",", ".")) * 100) || 0; }

export function CommandCard({ canDelete, comanda, products, paymentMethods, debts }: { canDelete: boolean; comanda: { id: string; code: string; label: string; type: string; playerName?: string | null; items: CommandItem[] }; products: Product[]; paymentMethods: string[]; debts: Debt[] }) {
  const [pending, startTransition] = useTransition();
  const [productModalOpen, setProductModalOpen] = useState(false);
  const [checkoutOpen, setCheckoutOpen] = useState(false);
  const [divideOpen, setDivideOpen] = useState(false);
  const [productSearch, setProductSearch] = useState("");
  const [quantities, setQuantities] = useState<Record<string, number>>({});
  const [payments, setPayments] = useState<Payment[]>([]);
  const [includeOpenDebts, setIncludeOpenDebts] = useState(false);
  const [selectedDebtIds, setSelectedDebtIds] = useState<string[]>([]);
  const [splitCount, setSplitCount] = useState(2);
  const [message, setMessage] = useState("");
  const totalCents = comanda.items.reduce((total, item) => total + item.totalCents, 0);
  const paymentTotalCents = payments.reduce((total, payment) => total + toCents(payment.amount), 0);
  const selectedDebts = debts.filter((debt) => selectedDebtIds.includes(debt.id));
  const selectedDebtTotalCents = selectedDebts.reduce((total, debt) => total + debt.amountCents, 0);
  const checkoutTotalCents = totalCents + selectedDebtTotalCents;
  const comandaPaymentCents = Math.min(totalCents, Math.max(0, paymentTotalCents - selectedDebtTotalCents));
  const remainingCents = Math.max(0, totalCents - comandaPaymentCents);
  const normalizedSearch = productSearch.trim().toLocaleLowerCase("pt-BR");
  const productsByCategory = useMemo(() => products.reduce<Record<string, Product[]>>((groups, product) => {
    if (normalizedSearch && !product.name.toLocaleLowerCase("pt-BR").includes(normalizedSearch)) return groups;
    const categoryName = product.category?.name ?? "Sem categoria";
    (groups[categoryName] ??= []).push(product);
    return groups;
  }, {}), [normalizedSearch, products]);
  const run = (operation: () => Promise<void>, success?: () => void) => startTransition(async () => { try { setMessage(""); await operation(); success?.(); } catch (error) { setMessage(error instanceof Error ? error.message : "Não foi possível atualizar a comanda."); } });
  const form = (values: Record<string, string>) => { const data = new FormData(); Object.entries(values).forEach(([key, value]) => data.set(key, value)); return data; };
  const stop = (event: React.MouseEvent) => event.stopPropagation();
  const changeQuantity = (productId: string, value: number) => setQuantities((current) => ({ ...current, [productId]: Math.max(0, Math.min(value, products.find((product) => product.id === productId)?.stockQuantity ?? 0)) }));
  const addSelectedProducts = () => run(async () => {
    const selected = Object.entries(quantities).filter(([, quantity]) => quantity > 0);
    if (!selected.length) throw new Error("Selecione ao menos um produto.");
    for (const [productId, quantity] of selected) await addComandaProductAction(form({ comandaId: comanda.id, productId, quantity: String(quantity) }));
  }, () => { setQuantities({}); setProductSearch(""); setProductModalOpen(false); });
  const addPayment = () => setPayments((current) => [...current, { id: Date.now(), paymentMethod: paymentMethods[0] ?? "PIX", amount: "" }]);
  const toggleOpenDebts = (checked: boolean) => { setIncludeOpenDebts(checked); setSelectedDebtIds(checked ? debts.map((debt) => debt.id) : []); };
  const finish = () => run(() => finishComandaAction(form({ comandaId: comanda.id, payments: JSON.stringify(payments.map((payment) => ({ paymentMethod: payment.paymentMethod, amountCents: toCents(payment.amount) })).filter((payment) => payment.amountCents > 0)), debtIds: JSON.stringify(selectedDebtIds) })), () => setCheckoutOpen(false));
  const remove = () => run(() => deleteComandaAction(form({ comandaId: comanda.id })), () => setCheckoutOpen(false));

  return <article className="command-card command-card-clickable" onClick={() => setCheckoutOpen(true)}>
    <header><div><strong>{comanda.label}</strong><span>{comanda.type === "AVULSA" ? "Avulsa" : comanda.playerName ?? "Cliente"}</span></div><small>{comanda.code}</small></header>
    <div className="command-items">{comanda.items.length ? comanda.items.map((item) => <div className="command-item" key={item.id}><span><b>{item.quantity}×</b> {item.product.name}</span><strong>{money(item.totalCents)}</strong><div className="command-item-controls" onClick={stop}><button type="button" aria-label={`Diminuir ${item.product.name}`} onClick={() => run(() => updateComandaItemQuantityAction(form({ itemId: item.id, delta: "-1" })))} disabled={pending}>−</button><button type="button" aria-label={`Aumentar ${item.product.name}`} onClick={() => run(() => updateComandaItemQuantityAction(form({ itemId: item.id, delta: "1" })))} disabled={pending}>+</button></div></div>) : <p className="command-items-empty">Nenhum produto inserido.</p>}</div>
    <footer><div><span>Total atual</span><strong>{money(totalCents)}</strong></div><div className="command-card-actions" onClick={stop}><button type="button" className="button" onClick={() => setProductModalOpen(true)}>Inserir produtos</button><button type="button" className="button button-success" disabled={pending} onClick={() => setCheckoutOpen(true)}>Finalizar comanda</button></div></footer>{message ? <p className="command-card-message">{message}</p> : null}

    {productModalOpen ? <div className="command-modal-backdrop" role="presentation" onMouseDown={() => setProductModalOpen(false)}><section className="command-product-modal" role="dialog" aria-modal="true" aria-label="Produtos em estoque" onMouseDown={(event) => event.stopPropagation()}><header><div><span>ESTOQUE</span><h2>Produtos em estoque</h2></div><button type="button" className="button button-small" onClick={() => setProductModalOpen(false)}>Fechar</button></header><label className="command-product-search">Buscar produto<input autoFocus value={productSearch} onChange={(event) => setProductSearch(event.target.value)} placeholder="Digite o nome do produto" /></label><div className="command-product-kanban">{Object.entries(productsByCategory).map(([categoryName, categoryProducts]) => <section key={categoryName} className="command-product-column"><h3>{categoryName}</h3><div>{categoryProducts.map((product) => <article key={product.id} className={product.stockQuantity ? "command-product-row" : "command-product-row command-product-row-disabled"}><div><strong>{product.name}</strong><span>{money(product.priceCents)} · {product.stockQuantity} em estoque</span></div><label>Quantidade<input type="number" min="0" max={product.stockQuantity} value={quantities[product.id] ?? 0} disabled={!product.stockQuantity} onChange={(event) => changeQuantity(product.id, Number(event.target.value))} /></label></article>)}</div></section>)}</div>{!Object.keys(productsByCategory).length ? <p className="command-items-empty">Nenhum produto encontrado.</p> : null}<footer><button type="button" className="button" onClick={() => setProductModalOpen(false)}>Cancelar</button><button type="button" className="button button-primary" disabled={pending} onClick={addSelectedProducts}>Adicionar selecionados</button></footer></section></div> : null}

    {checkoutOpen ? <div className="command-modal-backdrop" role="presentation" onMouseDown={() => setCheckoutOpen(false)}><section className="command-checkout-modal" role="dialog" aria-modal="true" aria-label="Finalizar comanda" onMouseDown={(event) => event.stopPropagation()}><header><div><span>FINALIZAÇÃO</span><h2>Finalizar comanda</h2></div><button type="button" className="button button-small" onClick={() => setCheckoutOpen(false)}>Fechar</button></header>{debts.length ? <section className="command-open-debts"><div><h3>Débitos em aberto</h3><p>Inclua os débitos anteriores nesta cobrança.</p></div><label className="control-toggle"><input type="checkbox" checked={includeOpenDebts} onChange={(event) => toggleOpenDebts(event.target.checked)} /><span aria-hidden="true" /><strong>Incluir débitos em aberto</strong></label>{includeOpenDebts ? debts.map((debt) => <label key={debt.id}><input type="checkbox" checked={selectedDebtIds.includes(debt.id)} onChange={(event) => setSelectedDebtIds((current) => event.target.checked ? [...current, debt.id] : current.filter((id) => id !== debt.id))} /><span><strong>{debt.description}</strong><small>{debt.dueDate ? `Vencimento: ${new Intl.DateTimeFormat("pt-BR").format(debt.dueDate)}` : "Em aberto"}</small></span><b>{money(debt.amountCents)}</b><em>Selecionar para quitar junto</em></label>) : null}</section> : null}<div className="command-checkout-grid"><section><h3>Resumo da comanda</h3><dl><div><dt>Cliente</dt><dd>{comanda.label}</dd></div><div><dt>Itens</dt><dd>{comanda.items.reduce((total, item) => total + item.quantity, 0)} produto(s)</dd></div><div><dt>Comanda</dt><dd>{money(totalCents)}</dd></div><div><dt>Débitos selecionados</dt><dd>{money(selectedDebtTotalCents)}</dd></div><div><dt>Total a receber</dt><dd>{money(checkoutTotalCents)}</dd></div></dl></section><section><div className="command-checkout-payment-head"><div><h3>Formas de pagamento</h3><p>Divida o valor entre quantas formas precisar.</p></div><button type="button" className="button button-small" onClick={addPayment}>Adicionar forma de pagamento</button></div>{payments.length ? <div className="command-payment-list">{payments.map((payment) => <div key={payment.id} className="command-payment-row"><select value={payment.paymentMethod} onChange={(event) => setPayments((current) => current.map((item) => item.id === payment.id ? { ...item, paymentMethod: event.target.value } : item))}>{paymentMethods.map((method) => <option key={method} value={method}>{method}</option>)}</select><input inputMode="decimal" value={payment.amount} placeholder="0,00" onChange={(event) => setPayments((current) => current.map((item) => item.id === payment.id ? { ...item, amount: event.target.value } : item))} /><button type="button" aria-label="Remover forma de pagamento" onClick={() => setPayments((current) => current.filter((item) => item.id !== payment.id))}>×</button></div>)}</div> : <p className="command-checkout-warning">Nenhuma forma de pagamento informada. A conta será criada em aberto no Contas a Receber.</p>}{selectedDebts.length ? <p className="command-checkout-warning">O pagamento é aplicado primeiro aos débitos selecionados. Cada baixa parcial preserva o valor original e registra o saldo no histórico.</p> : null}<div className="command-checkout-totals"><span>Recebido: <strong>{money(paymentTotalCents)}</strong></span><span>Conta a receber: <strong>{money(remainingCents)}</strong></span></div></section></div><footer><div className="command-checkout-danger-actions">{canDelete ? <button type="button" className="button button-danger" disabled={pending} onClick={remove}>Apagar comanda</button> : null}<button type="button" className="button button-warning" onClick={() => setDivideOpen(true)}>DIVIDIR COMANDA</button></div><div><button type="button" className="button" onClick={() => setCheckoutOpen(false)}>Cancelar</button><button type="button" className="button button-success" disabled={pending || paymentTotalCents > checkoutTotalCents} onClick={finish}>{remainingCents ? "Finalizar e deixar saldo em aberto" : "Confirmar pagamento"}</button></div></footer>{divideOpen ? <div className="command-divide-popover" role="dialog" aria-label="Dividir comanda"><strong>Dividir comanda</strong><p>Defina em quantas pessoas a conta será dividida. Use as formas de pagamento para informar a baixa de cada parte.</p><label>Participantes<input type="number" min="2" max="20" value={splitCount} onChange={(event) => setSplitCount(Math.max(2, Number(event.target.value) || 2))} /></label><b>{money(Math.floor(checkoutTotalCents / splitCount))} por pessoa</b><button type="button" className="button button-small" onClick={() => setDivideOpen(false)}>Concluir</button></div> : null}</section></div> : null}
  </article>;
}
